{
  "name": "pi-hypercharm-provider",
  "version": "1.3.22",
  "description": "HyperCharm provider extension for pi - Access DeepSeek, GLM, Kimi, Qwen, MiniMax, Gemma, and GPT-OSS models through the Charm Hyper API",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "clean": "echo 'nothing to clean'",
    "build": "echo 'nothing to build'",
    "check": "tsc --noEmit && node tests/status.smoke.ts && node tests/sidebar.smoke.ts && bun run tests/routing.smoke.ts",
    "smoke": "node tests/status.smoke.ts && node tests/sidebar.smoke.ts && bun run tests/routing.smoke.ts",
    "update-models": "node scripts/update-models.js"
  },
  "keywords": [
    "pi",
    "extension",
    "provider",
    "hypercharm",
    "charm",
    "hyper",
    "ai",
    "llm",
    "kimi",
    "deepseek",
    "glm",
    "qwen",
    "minimax",
    "gemma",
    "gpt-oss"
  ],
  "devDependencies": {
    "@earendil-works/pi-ai": "0.85.1",
    "@earendil-works/pi-coding-agent": "0.85.1",
    "@types/node": "^24.10.1",
    "typescript": "6.0.3"
  },
  "author": "",
  "license": "MIT",
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
